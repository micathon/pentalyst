do (
gdefun (var mygvar) do (
  p 3;
);
defun (p x)(var i n) do (
  set n x;
  println "else: top";
  if false do (
    println "if";
	return;
  )
  elif false do (
    println "elif";
	return;
  )
  else do (
    println "else";
	return;
  );
  for (= i 0; ? (< i n); ++ i) do (
    println "p: i =" i;
  );
);
)
