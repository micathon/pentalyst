do (
gdefun (var mygvar) do (
  p 3;
);
defun (p x)(var i n) do (
  #set n x;
  println "switch: top";
  switch 1
  case 0 do (
    return;
  )
  case 1 do (
    println "case - skipped";
	return;
  );
  for (= i 0; ? false; ++ i) do (
    #println "p: i =" i;
  );
);
)
