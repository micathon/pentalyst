do (
gdefun (var mygvar) do (
  p 3;
);
defun (p x)(var i n) do (
  set n x;
  println "for: top";
  if false do (
    println "if";
	return;
  )
  elif false do (
    println "elif";
	return;
  );
  for (= i 0; ? (< i n); ++ i) do (
    println "p: i =" i;
  );
);
)
